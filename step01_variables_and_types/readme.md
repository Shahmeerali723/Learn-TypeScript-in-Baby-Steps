# Variables and Types Steps:

## let or const

let: Declares a mutable variable, which means its value can change.

const: Declares an immutable variable, which means its value can't be changed after it's assigned.

## Data Types:

